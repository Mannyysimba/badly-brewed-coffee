node_modules
.next
.env
.env.local
# Ship the seeded demo DB (data/bbc.db) so Vercel has users to sign in against.
# Side files (WAL/shm) stay ignored — they're runtime-only.
data/*.db-journal
data/*.db-wal
data/*.db-shm
drizzle/
next-env.d.ts
.DS_Store
*.tsbuildinfo

# Team 17 — raw source assets (design drop). The versions used at runtime
# are copied into /public; keep the originals off the repo to avoid bloat.
/asset/
/Team_*.pdf

.vercel
